# G1: 管道 Pipe (Day 36)

## 对照源码
- reference/linux-7.1/fs/pipe.c

## 学习目标
1. pipe() + dup2() 系统调用\n2. 环形缓冲区 (head/tail/count)\n3. Shell 管道解析 '|'

## 代码文件
- kernel/fd.c — pipe/dup2 + 环形缓冲区\n- arch/x86/entry.S — syscall 11(pipe), 12(dup2)

## 验证
```bash
cd course/day36 && make
qemu-system-x86_64 -kernel mykernel.elf -display none -serial stdio -no-reboot -no-shutdown
```
