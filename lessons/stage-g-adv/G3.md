# G3: TCP 协议栈 (Day 38)

## 对照源码
> **状态**: QEMU验证通过 ✓
- reference/linux-7.1/net/ipv4/tcp.c

## 学习目标
> **状态**: QEMU验证通过 ✓
1. TCP 首部结构 (端口/序列号/标志位)\n2. SYN 包构建和发送\n3. TCP over IP over Ethernet 分层

## 代码文件
> **状态**: QEMU验证通过 ✓
- net/tcp.c — TCP 首部结构 + SYN 发送\n- net/net.c — IP/以太网层

## 验证
> **状态**: QEMU验证通过 ✓
```bash
cd course/day38 && make
qemu-system-x86_64 -kernel mykernel.elf -display none -serial stdio -no-reboot -no-shutdown
```
