# G4: HTTP 协议 (Day 39)

## 对照源码
> **状态**: QEMU验证通过 ✓
- RFC 7230 (用户态协议)

## 学习目标
> **状态**: QEMU验证通过 ✓
1. HTTP/1.1 GET 请求格式\n2. 应用层→传输层→网络层→链路层\n3. HTTP 响应解析

## 代码文件
> **状态**: QEMU验证通过 ✓
- net/http.c — HTTP GET 请求构建\n- net/tcp.c — TCP 传输

## 验证
> **状态**: QEMU验证通过 ✓
```bash
cd course/day39 && make
qemu-system-x86_64 -kernel mykernel.elf -display none -serial stdio -no-reboot -no-shutdown
```
