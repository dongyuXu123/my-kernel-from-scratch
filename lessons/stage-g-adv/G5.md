# G5: Framebuffer 控制台 (Day 40)

## 对照源码
> **状态**: QEMU验证通过 ✓
- PCI VGA 规范, 8x16 位图字体

## 学习目标
> **状态**: QEMU验证通过 ✓
1. PCI VGA 探测 (vendor=0x1234 device=0x1111)\n2. 8x16 位图字体渲染\n3. fb_putchar/fb_puts 终端输出

## 代码文件
> **状态**: QEMU验证通过 ✓
- arch/x86/fb.c — VGA 探测 + 字体渲染\n- mm/page.c — map_mmio 映射

## 验证
> **状态**: QEMU验证通过 ✓
```bash
cd course/day40 && make
qemu-system-x86_64 -kernel mykernel.elf -display none -serial stdio -no-reboot -no-shutdown
```
